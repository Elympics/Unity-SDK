namespace Elympics
{
    public enum LogLevel
    {
        // the playpad logger takes these in as numbers - watch out when changing the order
        Info,
        Warning,
        Error,
        Exception,
        Debug,
        Trace,
    }
}
